<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_search"
        android:icon="@drawable/ic_bluetooth_white_24dp"
        app:showAsAction="ifRoom|withText"
        android:title="@string/action_bluetooth" />

    <item
        android:id="@+id/action_terminal"
        app:showAsAction="never"
        android:title="@string/terminal" />

    <item
        android:id="@+id/action_registration"
        app:showAsAction="never"
        android:title="@string/registeration" />

    <item
        android:id="@+id/action_my_type"
        app:showAsAction="never"
        android:title="@string/my_type" />

</menu>